코루틴 싱글톤 클래스
-코루틴 호출용 싱글톤 클래스

마우스 컨트롤러 클래스
-마우스 클릭 시 기준점 저장
-마우스 드래그 상태인지 체크
-드래그 상태일 경우 기준점과 좌표를 이용하여 플레이어 이동


플레이어 매니저 컨트롤러 클래스
-(무브 매개)좌표를 집어넣으면->트랜스폼의 좌표를 해당 좌표로 변경
-(라이프 매개)값을 집어넣으면->라이프 증감
-(스코어 매개)값을 집어넣으면->스코어 증감
-충돌 시(아이템 획득 매개)충돌 객체를 집어넣으면->충돌한 객체의 효과 온

무브 모델 클래스
-트랜스폼 세팅
-애리어 세팅
-좌표를 집어넣으면->트랜스폼의 좌표를 해당 좌표로 변경
1. (애리어 매개)애리어 내부에서만 이동
2. 애리어 무시하고 이동
-(애리어 매개)좌표를 집어넣으면->애리어 내부인지 리턴

애리어 모델 클래스
-스퀘어 세팅
-좌표를 집어넣으면->애리어 내부로 좌표 수정
-좌표를 집어넣으면->애리어 내부인지 리턴

스퀘어 구조체
-좌하, 우상 포인트 두 개 저장

포인트 구조체
-x, y 좌표값 저장

라이프 모델 클래스
-값을 집어넣으면->라이프 증감
-라이프에 변동이 있으면->(UI 매개)하트 갯수 증감
-라이프가 0 이하이면->(이벤트 매개)게임 오버 이벤트 호출

스코어 모델 클래스
-값을 집어넣으면->점수 증감
-점수에 변동이 있으면->(UI 매개)점수 숫자 변경


아이템 획득 컨트롤러 클래스
-(아이템 컨트롤러 매개)충돌 객체를 집어넣으면->충돌한 객체에 최상위 컨트롤러를 넘겨주며 효과 온
-(콤보 매개)콤보 갱신
1. 콤보 객체일 때 콤보 연장
2. 콤보 객체가 아닐 때 콤보 중단

콤보 모델 클래스
-콤보 연장
-콤보 중단
-콤보 시간 다 되면 중단
-현재 콤보 리턴


아이템 컨트롤러 클래스
-이펙터 리스트 세팅
-이펙터 수 반환
-(이펙터 매개)충돌한 객체의 최상위 컨트롤러를 넘겨주면->리스트의 각 이펙터들 실행
-(무브 매개)아이템 매니저에서 속도 값 가져와 무브 이용하여 아래로 이동
-(애리어 매개)애리어 밖으로 나갈 시(오브젝트 풀 매개)해당 아이템 릴리즈

이펙터 모델 클래스
-이펙터 부모 객체 세팅
-충돌한 객체의 플레이어 컨트롤러를 넘겨주면->가상함수 실행

(이펙터 상속)디스트로이어 모델 클래스
-가상함수 구현: (오브젝트풀 매개)부모 객체를 릴리즈

(이펙터 상속)애드스코어 모델 클래스
-가상함수 구현: (플레이어 매개)값을 집어넣으면->스코어 증감

(이펙터 상속)애드라이프 모델 클래스
-가상함수 구현: (플레이어 매개)값을 집어넣으면->라이프 증감


데이터 세이브 모델 클래스
-점수를 넣으면->최고값 갱신
-캐릭터 종류를 넣으면->열린 캐릭터 저장


아이템 매니저 컨트롤러 클래스
-주기와 크리에이터, 태그를 집어넣으면->주기마다 랜덤한 위치에 (크리에이터 매개)아이템 생성
-태그를 넣으면->해당 코루틴 정지

아이템 크리에이터 모델 클래스
-프리팹 경로 집어넣으면->(오브젝트 풀 매개)오브젝트 풀 생성
-이펙터 집어넣으면->(추가할)이팩터 리스트에 추가
-크리에이터의 이펙터 수와 (아이템 컨트롤러 매개)아이템의 이펙터 수를 비교하여 (오브젝트 풀 매개)아이템 생성 및 반환


이벤트 매니저 컨트롤러 클래스
-(이니셜라이즈 이벤트 매개)초기화 이벤트 호출
-(게임 스피드 매개)게임 스피드 상승 이벤트 호출
-(데드 이벤트 매개)죽음 이벤트 호출

이니셜라이즈 이벤트 모델 클래스
-해상도 세팅
-(아이템 디스펜서 매개)아이템 디스펜서 세팅

게임 스피드 모델 클래스
-초기 게임 속도 세팅
-주기와 증가속도를 집어넣으면->게임 속도 증가 코루틴 실행

데드 이벤트 모델 클래스
-(아이템 디스펜서 매개)아이템 디스펜서 코루틴 정지


*오브젝트 풀
-오브젝트 풀 전용 객체 클래스 만들기
-해당 객체가 자신의 오브젝트 풀을 갖도록 함